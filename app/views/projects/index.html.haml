%p.tagline= Settings.home_page_blurb

%ul#projects
  - @projects.each do |project|
    %li
      = link_to project do
        .image
          - if project.image.present?
            = image_tag project.image.url(:medium)
        .info
          %h2= project.title
          %p.description= project.description