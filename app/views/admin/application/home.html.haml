%h2 Home

= link_to 'Projects', admin_projects_path

%h3 Home page blurb
= form_tag(admin_blurb_path, method: :post) do
  .field
    = text_area_tag(:blurb, Settings.home_page_blurb)

  = submit_tag 'Save'
